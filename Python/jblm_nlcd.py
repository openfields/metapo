#!/usr/bin/env python
#
##############################################################################
#
# MODULE:       model
#
# AUTHOR(S):    Will Fields
#
# PURPOSE:      Delineates habitat networks for Joint Base Lewis-McChord
#
# DATE:         Wed Sep 12, 2018
#
##############################################################################

#%module
#% description: Script generated by wxGUI Graphical Modeler.
#%end

import sys
import os
import atexit

from grass.script import parser, run_command

def cleanup():
    pass
# 
def main():
# Set region & mask
  run_command("g.region",
                vector = 'jblm_cr_65r')

  run_command("r.mask",
                maskcats = "*",
                vector = 'jblm_cr_65r',
                layer = "1")

# # mapcalcs for habitat networks from rasters
  run_command("r.mapcalc",
                expression = 'jblm_cr_65r' + "_tmpvd=if(us_140vdep<67,1,null())",
                overwrite = True)
# #  
  run_command("r.mapcalc",
                 expression = 'jblm_cr_65r' + "_tmp42=if(nlcd_2011v14@PERMANENT==42,1,null())",
                 overwrite = True)

  run_command("r.mapcalc",
                expression = 'jblm_cr_65r' + "_tmp71=if(nlcd_2011v14@PERMANENT==71,1,null())",
                overwrite = True)
                

# multiply vdep & nlcd
  run_command("r.mapcalc",
                expression = 'jblm_cr_65r' + "_42=" + 'jblm_cr_65r' + "_tmpvd * " + 'jblm_cr_65r' + "_tmp42",
                overwrite = True)

  run_command("r.mapcalc",
                expression = 'jblm_cr_65r' + "_71=" + 'jblm_cr_65r' + "_tmpvd * " + 'jblm_cr_65r' + "_tmp71",
                overwrite = True)
                

# write data to vector       
  run_command("r.to.vect",
                input = 'jblm_cr_65r_42',
                output = 'jblm_cr_65r_42',
                type= "area",
                overwrite = True)         

  run_command("r.to.vect",
                input = 'jblm_cr_65r_71',
                output = 'jblm_cr_65r_71',
                type= "area",
                overwrite = True)         



# remove raster mask
  run_command("r.mask",
                flags = 'r',
                maskcats = "*",
                layer = "1")

  return 0

if __name__ == "__main__":
    options, flags = parser()
    atexit.register(cleanup)
    sys.exit(main())
